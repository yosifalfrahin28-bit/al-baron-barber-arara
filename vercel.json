{
  "buildCommand": "pnpm --filter @workspace/al-baron-mobile run build && rm -rf dist && cp -R artifacts/al-baron-mobile/dist dist",
  "outputDirectory": "dist",
  "installCommand": "corepack enable && pnpm install --frozen-lockfile",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/((?!assets/|api/).*)",
      "destination": "/index.html"
    }
  ]
}